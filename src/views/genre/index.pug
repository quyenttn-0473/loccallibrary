extends ../layout

block content
    .container 
        a.btn.btn-primary(href="/") #{t('book-page.btn_back')}
        h1 #{t('title-page.genre')}
        hr
        include ../partials/notifications
        h2 #{t('title-list.genre')}
        ul
            each genre in genres 
                li.disc
                    a(href="#") #{genre.name}

